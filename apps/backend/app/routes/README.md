# 🚦 Routes (API Endpoints)

이 폴더는 Flask의 **블루프린트(Blueprint)** 를 사용하여 애플리케이션의 모든 API 엔드포인트(라우터)를 정의하고 관리합니다.

## 🎯 핵심 역할

* `@analysis_bp.route(...)`와 같이 URL 경로와 HTTP 메서드(POST, GET 등)를 정의합니다.
* 사용자로부터 HTTP 요청(Request)을 받습니다.
* 요청 데이터를 검증하고, 실제 비즈니스 로직을 처리하기 위해 `services` 레이어의 함수를 호출합니다.
* `services`로부터 받은 결과를 `jsonify`하여 클라이언트(프론트엔드)에게 응답(Response)합니다.

이 폴더의 파일들은 **실제 AI 로직이나 데이터베이스 처리를 직접 수행하지 않습니다.** 오직 '교통 정리'의 역할만 담당합니다.
